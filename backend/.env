# --- API ---
PORT=4000
JWT_SECRET=4f9c2d78a1b6e3f59082c47d8123ef65b90714c3e8095ad1f2679c351b84920e
JWT_EXPIRES_IN=8h

# --- Postgres ---
DATABASE_URL=postgres://ddas:ddas_dev_password@localhost:5433/ddas

# --- Redis (BullMQ + rate limiting) ---
REDIS_URL=redis://localhost:6379

# --- MinIO (S3-compatible object storage) ---
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=ddas_admin
MINIO_SECRET_KEY=ddas_dev_password
MINIO_BUCKET=ddas-datasets

# --- Elasticsearch ---
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_INDEX=datasets

# --- Encryption at rest ---
# 32-byte key, hex-encoded (64 hex chars). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FILE_ENCRYPTION_KEY=8818a0ea9afd1e3536eac4d73faaa8dd2e34b9e6d74fcb3d0c6480ac861b3557
