# Copy this to .env before running docker compose up

# --- API ---
PORT=4000
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h

# --- Postgres ---
DATABASE_URL=postgres://ddas:ddas_dev_password@postgres:5432/ddas

# --- Redis (BullMQ + rate limiting) ---
REDIS_URL=redis://redis:6379

# --- MinIO (S3-compatible object storage) ---
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=ddas_admin
MINIO_SECRET_KEY=ddas_dev_password
MINIO_BUCKET=ddas-datasets

# --- Elasticsearch ---
ELASTICSEARCH_URL=http://elasticsearch:9200
ELASTICSEARCH_INDEX=datasets

# --- Encryption at rest ---
# 32-byte key, hex-encoded (64 hex chars). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FILE_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARS
